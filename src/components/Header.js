import classes from './Header.module.css'

const Header = (props) => {
    return <div className={classes.navbar}>
        <p className={classes.p}>DogShop</p>
    </div>
}

export default Header